{
    "scripts": {
        "dev": "npm link ../pkg && webpack serve",
        "build": "npm unlink --no-save glife-wasm && npm ci && rm -rf dist && webpack --mode production"
    },
    "dependencies": {
        "glife-wasm": "^0.4.0"
    },
    "devDependencies": {
        "@swc/cli": "^0.1.57",
        "@swc/core": "^1.3.4",
        "@tsconfig/recommended": "^1.0.1",
        "copy-webpack-plugin": "^11.0.0",
        "swc-loader": "^0.2.3",
        "webpack": "^5.74.0",
        "webpack-cli": "^4.10.0",
        "webpack-dev-server": "^4.10.0"
    }
}
